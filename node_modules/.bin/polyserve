#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../polyserve/bin/polyserve" "$@"
  ret=$?
else 
  node  "$basedir/../polyserve/bin/polyserve" "$@"
  ret=$?
fi
exit $ret
